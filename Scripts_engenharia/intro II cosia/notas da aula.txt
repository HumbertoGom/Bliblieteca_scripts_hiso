M=[10,20,30;
   40,50,60]

clear/clc

#limpar

close

#fechar janelas


vetor linha

v=[10,20,30]
(1x3)

vetor coluna

w=[100;200;300]
(3x1)

//comentário código não lido


modificar atribuir valores na matriz;

M(posição da linha, posição da coluna)


o uso de ':' marca toda lina ou coluna
M(:,1) vale 600 e 20


COMANDOS DE CRIAÇÃO DE MATRIZ[

zeros(l,c) - cria matriz nula 
ones(l,c) - cria matriz inteira igual a 1.
rand(l,c) - cria matriz com números aleatórios.
eye(l,c) - cria matriz identidade



para criar uma matriz com função
1-parte de uma matriz nula de mesma ordem
M(i,j) = i + j^2
	for i=1:5
		for j=1:3



multipliocação de matrizes

colunas da primeira = linhas da segunda


C= A*B


cada linha de uma matriz ou coluna podem ser interpretados como vetores linha ou vetores coluna.
se isso ocorrer,retornara um produto escalar


disciplina ciência dados,


matriz transposta
troca linhas por colunas e etc.

A=[10,20,30;
40,50,60]
disp(A') -->(') operador de transposta


máximo e mínimo

[vmax,pmax]= max(A)

retorna o valro e posição de máximo



absoluto da matriz - retira o sional(módulo de cada termo?)

abs(M)=


















sistemas lineares

#ele se arrepende de não ter aprendido linearidade antes
🎵Spagonia Day - Sonic Unleashed

A11x1 + A12x2 + ... + A1mXm = y1

A21x1 + A22x2 + ... + A2mXm = y2

A31x1 + A32x2 + ... + A3mXm = y3f

...      ...    ...    ...    ...

An1x1 + An2x2 + ... + AnmXm = yn


Na forma matricial

a11   a12  ...  a1m       x1         y1
a21   a22  ...  a2m       x2   =     y2
...   ...  ...  ...   *   ...        ...
an1   an2  ...  anm       xm         ym


A(nxm)                   X(mx1)  =  y(mx1)




eu nem vou mais anotar isso

as dimensões podem estar errada dependência linear

caso n<m , múltiplos x que resolvem


Eliminação gaussiana


primeiro ele monta uma matriz estendida lembro disso

fazer operações elementares afim d e transformar nessa matriz em uma identidade?
não trabalha com troca de linha ou de coluna?


A=[2,5,-5;
  -1,3,1;
  0,2,-4]

Y=[6;-4;2]

M = [A,y]  - isso forma a matriz extendida

operações unitárias não alteram o sistema

pega uma entrada doiagonal,chamada de pivot, usa a primeira linha e segunda linha para zerar as linhas abaixo.
avança uma diagonal e usa para zerrar o resto da coluna, repete até formar uma identidade.


pivo = M(1,1)
M(1,:) = M(1,:)/pivô   ==> divide todos pelo pivô para pivo=1.

M(2,:) = M(2,:) - M(1,:)* M(2,1) 

M(3,:) = M(3,:) - M(1,:) *M(3,1) 

essas operações ajeitam a primeira coluna para 0.


pivo = M(2,2)
M(2,:) = M(2,:)/pivô   ==> pivô da segunda coluna é definido como 1.

M(1,:) = M(1,:) - M(2,:)* M(2,2) 

M(3,:) = M(3,:) - M(2,:) *M(3,2) 


segunda coluna é anulada exeto no 2,2

for